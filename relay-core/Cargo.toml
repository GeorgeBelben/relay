[package]
name = "relay-core"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = ["rt-multi-thread", "macros", "net", "io-util"] }
serde_json = "1"
relay-protocol = { path = "../relay-protocol" }